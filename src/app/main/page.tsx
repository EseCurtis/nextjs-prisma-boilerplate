"use client";
import { useAuth } from "@/hooks/api/use-auth";

export default function Dashboard() {
  const { } = useAuth();

  return (
    <div className="p-3">
      <h1 className="text-3xl">
       
      </h1>
    </div>
  );
}
